import { Box, styled } from "@mui/material";

export const ProgressContainer = styled(Box)(() => ({
    display: 'flex', 
    justifyContent: 'center',
    padding: '20px 0',
    marginTop: '20px'
}))